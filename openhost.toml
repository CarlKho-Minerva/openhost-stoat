[app]
name = "stoat"
version = "0.1.0"
description = "Unified Stoat chat stack on OpenHost"

[runtime.container]
image = "Dockerfile"
port = 8000

[routing]
health_check = "/"
public_paths = ["/"]

[resources]
memory_mb = 1536
cpu_cores = 1.5

[data]
app_data = true
app_archive = true
