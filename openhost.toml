[app]
name = "stoat"
version = "0.1.0"
description = "Self-hosted Stoat chat platform on OpenHost"

[runtime.container]
image = "Dockerfile"
port = 8000

[routing]
health_check = "/"
public_paths = ["/"]

[resources]
memory_mb = 1024
cpu_cores = 1.0

[data]
app_data = true
app_archive = true
