[app]
name = "stoat"
version = "0.1.0"
description = "Self-hosted Stoat web client on OpenHost"

[runtime.container]
image = "Dockerfile"
port = 80

[routing]
health_check = "/"
public_paths = ["/"]

[resources]
memory_mb = 512
cpu_cores = 0.5

[data]
app_data = true
app_archive = true
